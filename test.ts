export class archainTS {
    constructor(port, settings) {
        this.port = port;
        this.settings = settings;
    }
    hello() {
        let text:string = "Hello World"
        console.log(text);
    }
}