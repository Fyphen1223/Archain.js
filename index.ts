const express = require("express");
const server = express();

export class archain {
    constructor(num:port, json:settings) {
        this.port = port;
        this.settings = settings;
    }

}